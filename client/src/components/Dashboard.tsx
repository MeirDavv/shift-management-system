import React from "react";
import { Logout } from "./Logout";

export const Dashboard = () => {

  return (
    <div>
        <nav className="sideNav">
            <Logout></Logout>
        </nav>
    </div>
  );
};
